

def send_sms(user, msg):
    print('send sms to {} \t msg: {}'.format(user.username, msg))
    pass


def send_email(user, msg):
    print('send email to {} \t msg: {}'.format(user.username, msg))
    pass
